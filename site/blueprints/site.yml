title: Site

tabs:
  pages:
    label: Pages
    icon: page
    sections:
      frontpage:
        type: pages
        headline: Frontpage
        layout: cardlets
        image: icon
        template: home
      pages:
        type: pages
        headline: Subpages
        layout: cardlets
        image: icon
        template:
          - default
          - page
          - about
          - contact
          - roadmap
      pages_collections:
        type: pages
        headline: Collections
        layout: cardlets
        image: icon
        template:
          - best-practices

  navigation:
    extends: tabs/navigation

  branding:
    extends: tabs/branding

  admin:
    extends: tabs/admin

  popup:
    extends: tabs/popup
